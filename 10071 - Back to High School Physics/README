Status: Accepted

This algorithm scans for two integers. While the integers are valid (-100<=v<=100 & 0<= t <= 200), the program prints to stdin the displacement in twice the time. Displacement is *vt*, so the displacement two times the time is *2vt*.
